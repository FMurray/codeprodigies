
/**
 * Find the index of an array of integers where the 
 * sum of all the integers to the left equals the 
 * sum of all the integers to the right. If no such
 * index exists, return -1
 */

/**
 * EXAMPLE 1: 
 * findPivot([1, 3, 6, 6, 5, 5]) --> 3
 */

/**
 * EXAMPLE 2: 
 * findPivot([1, 2, 3]) --> -1
 */

/**
 * notes:
 * e.g. values will be in the range 0 <= n <= 100 
 */

function findPivot() {

}
